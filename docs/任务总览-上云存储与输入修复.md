# 任务总览 · 上云存储 + 输入无反应修复 + 上线自测

## 做了什么

**1. 上云数据存储方案（交付）**
- `docs/云上数据存储方案.md`：现状盘点 → 目标架构（数据收拢 `/data/bia` 独立数据盘）→ 分层备份 SOP → 云盘/实例选型 → P0-P2 必改项
- `docker-compose.prod.yml`：生产覆盖文件（Redis 开 AOF + MySQL/Redis/附件落盘），已通过 compose 校验

**2. 「询问没有反应」彻底修复（交付）**
- 根因：后端 TASK_BUSY 拒绝 + 前端失败静默吞错（输入清空/消息残留）+ 中文 IME 拦截
- 修改 `ChatView.vue`（失败回滚+分型提示+IME 兼容）、`App.vue`/`main.js`（toast 时长）、`task/router.py`（WS token 过期清理）
- ⚠️ 生效需 `docker compose up -d --build frontend backend`（当前运行容器是旧镜像）

**3. 上线级自测（交付）**
- `docs/上线自测报告.md`：smoke_test 全 PASS、真实归因质量验证通过（-58.96%）、WS 升级 101、演示数据可回放、前端构建通过
- 结论：达到**演示/试运行**级别；生产发布前需完成 4 个前置项（重建容器、Redis AOF、备份+恢复演练、密钥轮换+HTTPS）

## 关键发现
- 本机宿主机 **PID 22296 残留 python 进程占用 8000 端口**（旧 backend），会导致本地测试连错端口失败；真实链路是 8080(frontend)→docker backend(8001)
- 系统当前走**离线确定性分析**路径（LLM Key 未在运行时配置），单库场景质量已验证

## 下一步（待爸爸处理）
1. 重建容器让修复生效
2. 云服务器执行存储方案（数据盘 + Redis AOF + 备份）
3. 生产前密钥轮换 + HTTPS
